node_modules
dist
coverage
pnpm-lock.yaml
packages/core/migrations
# Docs contain hand-formatted tables/diagrams — leave them as authored.
*.md

# Self-contained Python subproject with its own tooling (ruff/pytest); the
# Webflow'd TypeScript formatter should not police its YAML/JSON.
govuk-rag-eval
